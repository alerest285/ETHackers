# Minimal deps for the Northflank training Job.
# Deliberately excludes torch / transformers / grounding-dino — the classifier
# itself only needs numpy + scikit-learn + joblib. The pipeline-produced JSONs
# (detections, scene_graphs) are supplied as training data.

numpy
scipy
scikit-learn
joblib
tqdm

# For fetch_training_data.py — pick the one matching your storage backend and
# drop the other to keep the image small.
boto3         ; sys_platform != "never"   # AWS S3 / any S3-compatible bucket
google-cloud-storage ; sys_platform == "never"   # disabled by default
